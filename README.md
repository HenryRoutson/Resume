# Resume

LaTeX resume built with MacTeX.

## Requirements

[MacTeX](https://www.tug.org/mactex/) — `brew install --cask mactex`

## Build

```
./setup.sh
```

Output PDF is written to `tex_resume_history/`.
